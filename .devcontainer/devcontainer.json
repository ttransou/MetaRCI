{
  "name": "MetaRCI",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y poppler-utils && python -m pip install --upgrade pip && python -m pip install -r requirements.txt"
}
